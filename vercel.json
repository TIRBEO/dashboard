{
  "installCommand": "rm -rf node_modules && npm install --legacy-peer-deps",
  "buildCommand": "pnpm --filter @tirbeo/dashboard build",
  "outputDirectory": ".next",
  "env": {
    "VERCEL_BUILD_TRACE": "false"
  }
}
